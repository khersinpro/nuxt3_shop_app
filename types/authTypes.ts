type User = {
    id: number,
    firstName: string,
    lastName: String,
    email: string,
    role_name: String
}

type Credentials = {
    email: string,
    password: string
}

type RegisterInfo = {
    firstName: String,
    lastName: String,
    email: String,
    password: String
}

